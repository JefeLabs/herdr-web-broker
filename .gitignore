node_modules/
dist/
.screenshots/
.idea
.env
# npm auth token — a granular access token with bypass-2FA lives here so
# `npm publish` needs no OTP and no TTY. NEVER commit it.
.npmrc
/docs/superpowers/
*.tsbuildinfo
.playwright-mcp/

# playwright e2e artifacts
test-results/
playwright-report/
